# PTO-ISA 原始资料

本目录存放 PTO-ISA 相关的**自写笔记、摘录与总结**（非代码仓镜像）。

## 用途

- 将从 `code/pto-isa-main` 或官方文档中理解到的要点，整理为 markdown 后放入此处
- 通过 `wiki-ingest` 编译进 `wiki/` 结构化知识层

## 约定

- 单篇文件命名：小写英文连字符，如 `comm-isa-overview.md`
- 新增文件后，在 `raw/index.md` 登记，再执行 ingest
- 不在此目录放置完整代码仓或 git submodule
